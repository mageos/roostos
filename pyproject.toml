[tool.uv.workspace]
members = ["roostos-engine", "roostos-sdk", "roostos-dns-technitium", "roostos-web"]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-httpx>=0.30.0",
]
